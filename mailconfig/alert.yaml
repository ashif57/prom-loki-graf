alertmanager:
  config:
    global:
      smtp_smarthost: "smtp.gmail.com:587"
      smtp_from: "ashifdummy57@gmail.com"
      smtp_auth_username: "ashifdummy57@gmail.com"
      smtp_auth_password:REMOVEDSECRET #remove the space between the password

    route:
      receiver: default-receiver
      routes:
        - matchers:
            - source="email-test"
          receiver: email-alerts

    receivers:
      - name: default-receiver

      - name: email-alerts
        email_configs:
          - to: "ashifdummy57@gmail.com"
